#Ignore the installed node modules since there are so many files
#use `npm i` instead to install these files.
node_modules/

#Ignore the config file so that personal DB settings are sent to the repo
config.js

#Ignore the folder generated by Komodo Edit
.komodotools/